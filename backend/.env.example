# Example .env file with actual credentials
# Copy this format and replace with YOUR actual MongoDB credentials

# Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB Connection - REPLACE WITH YOUR ACTUAL CREDENTIALS
# Example format (replace 'yourUsername' and 'yourPassword' with real values):
# MONGODB_URI=mongodb+srv://yourUsername:yourPassword@musicplayerclone.xs1mbx5.mongodb.net/spotify-clone?retryWrites=true&w=majority

# Current (needs to be updated):
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@musicplayerclone.xs1mbx5.mongodb.net/spotify-clone?retryWrites=true&w=majority

# JWT Secret (this is fine as-is)
JWT_SECRET=spotify_clone_super_secret_jwt_key_2025_music_app_authentication

# Client URL for CORS (this is fine as-is)
CLIENT_URL=*

# Session Secret (this is fine as-is)
SESSION_SECRET=your_session_secret_here

# INSTRUCTIONS:
# 1. Go to https://cloud.mongodb.com/
# 2. Sign in to your MongoDB Atlas account
# 3. Go to your cluster → Connect → Connect your application
# 4. Copy the connection string
# 5. Replace <username> and <password> with your actual database user credentials
# 6. Make sure the database name is "spotify-clone" at the end
# 7. Save this file and test with: node test-connection.js